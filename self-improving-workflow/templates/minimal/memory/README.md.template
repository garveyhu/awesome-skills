# 项目记忆库

由 `/self-improve` 维护的多层记忆。

## 结构

| 文件 / 目录 | 用途 | 是否进 git |
|-------------|------|------------|
| `semantic-patterns.json` | 抽象模式库（团队共享） | ✅（standard+ 档才有） |
| `episodic/YYYY-MM-DD-*.json` | 具体经验日志（个人临时） | ❌ |
| `working/*.json` | 当前会话上下文 | ❌（full 档才有） |

## 工作流

1. 开发中踩坑 / 学到 → `/self-improve` 触发
2. 抽取经验 → 写 episodic（如有 self-improving-agent skill）
3. 重复经验抽象成 pattern → 写 semantic-patterns.json
4. 高置信度 critical 级 → 用户确认后写入 `.claude/rules/dev-lessons.md`
5. 下次会话 CLAUDE.md 自动加载 → 行为改变 → 避免重复犯错

## 不要直接编辑

记忆文件由 skill 维护。如需手工调整规则，请编辑 `.claude/rules/*.md` 而非 `memory/`。
