---
description: 沉淀本会话的经验和教训到 .claude/rules/dev-lessons.md
---

# 自我改进协议

参数：$ARGUMENTS（可选 scope，例：`bug-fix-session` / `phase3` / 留空表示当前会话）

## 边界约束

1. **只写项目内**：禁止写 `~/.agents/`、`~/.claude/` 等全局目录
2. **写规则前必须用户确认**：`.claude/rules/*.md` 的任何修改都要 y/n
3. **最小依赖**：检查 `~/.agents/skills/self-improving-agent/` 是否存在
   - 存在 → 走完整三层记忆（episodic → semantic → rules）
   - 不存在 → 走 fallback 三问模式

## 流程

### Step 1：经验提取

分析本会话/本 scope：
- 做了什么（任务/skill/工具）
- 什么有效（what_went_well）
- 什么失败 + 根因（what_went_wrong + root_cause）
- 用户的反馈或修正

### Step 2：抽象成可复用规则

每条教训写成：
- **Rule**：一句话规则
- **Why**：根因/事件
- **How to apply**：什么时候应该想起这条

### Step 3：用户确认

列出抽取的 N 条教训，**等用户确认**哪些进 `dev-lessons.md`：

```
本次抽取 3 条教训：
1. [W/W?] 跨模块直接 Autowire 导致循环依赖 → 改用 Spring Event
2. [W/W?] Date import 漏了 22 个文件 → 模板化生成时统一 import 块
3. [W/W?] 评审 agent 串行跑太慢 → 改并行 dispatch

哪些写进 dev-lessons.md？(逗号分隔编号 / all / none)
```

### Step 4：写入

对用户确认的每一条：
- 追加到 `.claude/rules/dev-lessons.md`，带日期 + evolution marker
- 写入 `.claude/memory/episodic/YYYY-MM-DD-${ARGUMENTS:-session}.json`（若装了 self-improving-agent）

### Step 5：报告

```
✓ 已写入 2 条教训到 .claude/rules/dev-lessons.md
✓ 已存档 episodic 到 .claude/memory/episodic/2026-04-07-phase3.json
跳过 1 条（用户未确认）
```
