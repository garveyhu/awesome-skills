---
description: 阶段启动协议 — 计划 + 骨架 + 改造清单 + 用户确认
---

# Phase 启动协议

参数：$ARGUMENTS（例：`Phase4`、`AuthRefactor`）

执行以下步骤，**一步不能少**：

## 1. 写阶段计划

调用 `superpowers:writing-plans` 技能（如有），在 `docs/plans/` 创建 `YYYY-MM-DD-$ARGUMENTS-*.md`，内容包含：

- 本阶段目标 + 对应业务文档章节
- **接口/事件骨架清单**：所有需要新建的接口 / 事件 / Listener / Job，先建后填
- **改造清单**：本阶段所有需要修改的实现类列表
- 模块隔离方案（哪些 Agent 改哪些模块）
- 每个 Agent 完成后的编译验证步骤

## 2. 创建骨架先编译通过

按计划先创建所有空骨架文件（事件类、接口、监听器空壳），立即跑编译。

## 3. 用 TaskCreate 列改造清单

把每个待改造的实现类 / 接口创建成单独 task（用 TaskCreate）。**不允许漏项**。

## 4. 与用户确认

列出方案后等用户确认再开始派 Agent。**禁止跳过确认环节直接开干**。

---

**Why this protocol exists**：项目早期没有这个协议时，事件骨架后补、改造清单靠脑子记、Agent 跨模块乱改，导致 3 次返工。这个协议是踩坑后的硬性补救。
