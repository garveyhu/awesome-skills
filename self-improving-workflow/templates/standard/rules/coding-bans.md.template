---
description: 编码硬性禁令清单 — 写代码前必读
applies_to: 全部代码
---

# 编码硬性禁令

> 由用户维护。`/self-improve` 抽出的高置信度禁令在用户确认后追加到此处。

## 通用禁令（适用所有项目）

| # | 禁令 | 正确做法 |
|---|------|----------|
| 1 | ❌ Controller / Route 写业务逻辑 | ✅ 业务逻辑下沉到 Service 层 |
| 2 | ❌ 跨模块直接 import 对方实现类 | ✅ 走接口或事件解耦 |
| 3 | ❌ 改老的 migration 脚本 | ✅ 新增 migration 脚本 |
| 4 | ❌ 库存类字段 `set(余量 - n)` | ✅ 原子 SQL `WHERE 余量 >= n` |
| 5 | ❌ 敏感字段明文落库 | ✅ 加密注解 / hash |

## {{TECH_STACK}} 特定禁令

<!-- 由 init.sh 根据 tech_stack 参数注入对应栈的禁令 -->

## 项目特定禁令

<!-- 由 /self-improve 在用户确认后追加 -->
